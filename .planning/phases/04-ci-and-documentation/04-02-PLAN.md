---
phase: 04-ci-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]
user_setup: []
must_haves:
  truths:
    - "README has a Requirements section listing PHP 8.3+ and Laravel 11+"
    - "README has a version/compatibility matrix table"
    - "README installation section shows correct composer require syntax"
    - "CHANGELOG.md exists with Keep a Changelog format"
    - "CHANGELOG uses [Unreleased] header with Changed/Removed/Migration sections"
    - "CHANGELOG includes migration note about upgrading from 4.x"
  artifacts:
    - path: "README.md"
      provides: "Updated documentation with version requirements"
      min_lines: 50
    - path: "CHANGELOG.md"
      provides: "Release changelog for v5.0.0"
      min_lines: 20
  key_links:
    - "README Requirements section -> composer.json require constraints (source of truth)"
    - "CHANGELOG entries -> actual changes made in Phases 1-3"
---

# Plan 02: Documentation Updates

## Objective

Update README.md with current version requirements and compatibility matrix, and create CHANGELOG.md with Keep a Changelog format documenting the v5.0.0 modernization upgrade.

## Context

Current README state:
- No explicit Requirements section
- No version compatibility matrix
- Installation section just shows `composer require ringierimu/state-workflow`
- "Run Unit Test" section says `composer test` (correct, keep as-is)
- Rest of README documents configuration, usage, events, subscriber, artisan commands

Changes made in Phases 1-3 that CHANGELOG must document:
- Phase 1: PHP ^8.3, Laravel 11+/12+, Symfony 7, testbench ^9.15|^10.0, removed InstanceOfSupportStrategy shim
- Phase 2: Rector applied to src/ only, modernized PHP syntax
- Phase 3: Test suite migrated from PHPUnit to Pest, new test coverage added

From composer.json (source of truth):
- "php": "^8.3"
- "illuminate/*": "^11.0|^12.0"
- "symfony/workflow": "^7.0"
- "orchestra/testbench": "^9.15|^10.0"

<tasks>

<task type="auto">
  <name>Task 1: Update README with requirements and compatibility matrix</name>
  <files>README.md</files>
  <action>
    Make minimal, targeted changes to README.md:

    1. After the CI badge line and package description paragraph (before ## Installation), add a new ## Requirements section:
       ```markdown
       ## Requirements

       | Version | PHP | Laravel |
       |---------|-----|---------|
       | 5.x     | 8.3+ | 11, 12 |
       | 4.x     | 8.1+ | 10, 11 |

       ```

    2. Update the ## Installation section to show the requirement context:
       Replace the installation code block content. Keep the existing structure but update:
       ```markdown
       ## Installation

       Requires PHP 8.3+ and Laravel 11+.

       ```bash
       composer require ringierimu/state-workflow
       ```
       ```

       Keep the remaining publish config, publish migration, and run migration blocks exactly as-is.

    3. Do NOT modify any other sections (Configuration, Usage, Fired Event, Subscriber, Event Methods, Artisan Command, Run Unit Test, Credits).
  </action>
  <verify>
    grep -q "## Requirements" README.md — Requirements section exists
    grep -q "8.3+" README.md — PHP 8.3+ mentioned
    grep -q "11, 12" README.md — Laravel versions in table
    grep -q "5.x" README.md — Version 5.x row in compatibility table
    grep -q "4.x" README.md — Version 4.x row for historical reference
    grep -q "Requires PHP 8.3+ and Laravel 11+" README.md — Installation requirement note
    grep -q "composer require ringierimu/state-workflow" README.md — Install command preserved
    grep -q "composer test" README.md — Run Unit Test section preserved
  </verify>
  <done>README has Requirements section with compatibility matrix and updated installation section</done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <action>
    Create a new `CHANGELOG.md` at project root with Keep a Changelog format:

    ```markdown
    # Changelog

    All notable changes to `ringierimu/state-workflow` will be documented in this file.

    The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
    and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

    ## [Unreleased]

    ### Changed
    - Minimum PHP version raised to 8.3 (dropped 8.1, 8.2)
    - Minimum Laravel version raised to 11.0 (dropped Laravel 10)
    - Symfony dependencies updated to ^7.0 (dropped ^5.1, ^6.0)
    - Orchestra Testbench updated to ^9.15|^10.0
    - Test suite migrated from PHPUnit to Pest
    - CI matrix updated to PHP [8.3, 8.4] x Laravel [11, 12]

    ### Removed
    - PHP 8.1 and 8.2 support
    - Laravel 10 support
    - Symfony 5.x and 6.x support
    - `InstanceOfSupportStrategy` dual-import shim in WorkflowRegistry (Symfony 7 uses single import)

    > **Upgrading from 4.x:** Requires PHP 8.3+ and Laravel 11+. No public API changes — update your PHP and Laravel versions, then run `composer update`.
    ```

    Summary-level bullets aimed at package consumers. No contributor-level detail about Rector or individual test file changes.
  </action>
  <verify>
    test -f CHANGELOG.md — file exists
    grep -q "Keep a Changelog" CHANGELOG.md — format attribution present
    grep -q "\[Unreleased\]" CHANGELOG.md — uses Unreleased header
    grep -q "### Changed" CHANGELOG.md — Changed section present
    grep -q "### Removed" CHANGELOG.md — Removed section present
    grep -q "Upgrading from 4.x" CHANGELOG.md — migration note present
    grep -q "PHP 8.3" CHANGELOG.md — PHP version change documented
    grep -q "Laravel 10" CHANGELOG.md — Laravel 10 removal documented
  </verify>
  <done>CHANGELOG.md exists with Keep a Changelog format, [Unreleased] header, Changed/Removed sections, and migration note</done>
</task>

</tasks>

<verification>
```bash
# README Requirements section
grep "## Requirements" README.md

# README compatibility matrix
grep -A5 "| Version" README.md

# README installation note
grep "Requires PHP" README.md

# CHANGELOG exists
test -f CHANGELOG.md && echo "PASS" || echo "FAIL"

# CHANGELOG format
grep "Keep a Changelog" CHANGELOG.md
grep "\[Unreleased\]" CHANGELOG.md

# CHANGELOG migration note
grep "Upgrading from 4.x" CHANGELOG.md

# README Run Unit Test section unchanged
grep -A2 "## Run Unit Test" README.md
```
</verification>

<success_criteria>
- [ ] README has ## Requirements section with version/compatibility matrix table
- [ ] README installation section mentions PHP 8.3+ and Laravel 11+ requirements
- [ ] README `composer require` command preserved
- [ ] README "Run Unit Test" section with `composer test` is unchanged
- [ ] No other README sections modified
- [ ] CHANGELOG.md exists at project root
- [ ] CHANGELOG uses Keep a Changelog format with proper attribution
- [ ] CHANGELOG uses [Unreleased] header (not a version number)
- [ ] CHANGELOG has Changed and Removed sections
- [ ] CHANGELOG includes "Upgrading from 4.x" migration note
- [ ] CHANGELOG bullets are consumer-level, not contributor-level
</success_criteria>

<output>
After completion, create `.planning/phases/04-ci-and-documentation/04-02-SUMMARY.md`
</output>
