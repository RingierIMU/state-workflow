---
phase: 02-rector-pass
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - composer.json
  - composer.lock
  - rector.php
autonomous: true
requirements:
  - RECT-03

must_haves:
  truths:
    - "rector/rector is not in composer.json require-dev"
    - "driftingly/rector-laravel is not in composer.json require-dev"
    - "rector.php does not exist in the project root"
    - "composer.lock is clean and consistent with composer.json"
    - "The full PHPUnit test suite still passes after dependency removal"
  artifacts:
    - composer.json
    - composer.lock
  key_links:
    - "composer.json require-dev has no Rector entries"
    - "No rector.php config file exists"
---

<objective>
Remove Rector and driftingly/rector-laravel from dev dependencies and delete the rector.php configuration file.

Purpose: Clean up the one-time Rector tooling now that source modernization is complete. Rector is not kept as a permanent dependency per project decision.
Output: composer.json cleaned of Rector dependencies, rector.php deleted, lock file updated.
</objective>

<execution_context>
@/Users/matthew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rector-pass/02-01-SUMMARY.md

@composer.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Rector dependencies and config file</name>
  <files>composer.json, composer.lock, rector.php</files>
  <action>
1. **Remove Rector packages from dev dependencies:**
   ```bash
   composer remove --dev rector/rector driftingly/rector-laravel
   ```
   This updates both composer.json and composer.lock.

2. **Delete the rector.php configuration file:**
   ```bash
   rm rector.php
   ```

3. **Verify composer.json is clean:**
   ```bash
   grep -i rector composer.json
   ```
   Should produce NO output.

4. **Verify rector.php is gone:**
   ```bash
   test ! -f rector.php && echo "rector.php deleted"
   ```

5. **Run composer validate to ensure consistency:**
   ```bash
   composer validate --strict
   ```

6. **Run the full PHPUnit test suite** to confirm nothing broke:
   ```bash
   vendor/bin/phpunit
   ```

7. **Commit the cleanup** (second of two commits per user decision):
   ```bash
   git add composer.json composer.lock
   git rm rector.php 2>/dev/null || true
   git commit -m "chore: remove Rector and driftingly/rector-laravel from dev dependencies"
   ```
  </action>
  <verify>
    <automated>! grep -qi rector composer.json && test ! -f rector.php && vendor/bin/phpunit</automated>
  </verify>
  <done>rector/rector and driftingly/rector-laravel removed from composer.json, rector.php deleted, composer.lock updated, tests still pass, cleanup committed</done>
</task>

</tasks>

<verification>
- [ ] `grep -i rector composer.json` returns no output (RECT-03)
- [ ] `test ! -f rector.php` succeeds — config file deleted (RECT-03)
- [ ] `composer validate --strict` passes (lock file consistent)
- [ ] `vendor/bin/phpunit` exits 0 (tests still pass after removal)
</verification>

<success_criteria>
- composer.json require-dev has no rector/rector entry
- composer.json require-dev has no driftingly/rector-laravel entry
- rector.php does not exist in the project
- composer.lock is consistent with composer.json
- PHPUnit test suite passes
- Cleanup committed as separate commit from modernization
</success_criteria>

<output>
After completion, create `.planning/phases/02-rector-pass/02-02-SUMMARY.md`
</output>
